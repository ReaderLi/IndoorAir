<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_user_account"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="liruide.scu.com.indoorair.UserAccountActivity">

    <LinearLayout style="@style/wrap_layout"
        android:layout_marginTop="20dp"
        android:id="@+id/user_account_linearLin1"
        >
        <!-- 上圆角-->
        <LinearLayout
            android:id="@+id/user_account_username_lt"
            style="@style/top_layout">
            <TextView
                style="@style/user_account_text"
                android:text="@string/user_account_username" />
            <TextView
                style="@style/usertext"
                android:id="@+id/user_account_username_tv"
                android:text="你好"/>
        </LinearLayout>

        <!-- 分割线 -->
        <View style="@style/bg_line"/>
        <!-- 无圆角-->
        <LinearLayout
            android:id="@+id/user_account_old_password_lt"
            style="@style/mid_layout">
            <TextView style="@style/user_account_text" android:text="@string/user_account_old_password" />
            <EditText
                style="@style/usertext"
                android:id="@+id/user_account_old_password_lt_tv"
                android:text="你好"/>
        </LinearLayout>
        <View style="@style/bg_line"/>
        <!-- 下圆角-->
        <LinearLayout
            android:id="@+id/user_account_new_password_lt"
            style="@style/bottom_layout">
            <TextView style="@style/user_account_text" android:text="@string/user_account_new_password" />
            <EditText
                style="@style/usertext"
                android:id="@+id/user_account_new_password_tv"
                android:text="你好"/>
        </LinearLayout>

    </LinearLayout>


    <Button
        android:id="@+id/regBtn"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="400dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/tvForgot"
        android:text="注册"
        android:textColor="@color/white"
        android:background="@drawable/background_button_div"/>

</RelativeLayout>
